/**
 * Application routes constants.
 * Centralizes all routes to ensure consistency and ease of maintenance.
 */
export const ROUTES = {
  PROFILE: '/profile',
  LOGIN: '/login',
  REGISTER: '/register',
  HOME: '/'
} as const;
